<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.promanage.service.mapper.UserMapper">

    <!-- 根据用户名查找用户 -->
    <select id="findByUsername" resultType="com.promanage.service.entity.User">
        SELECT * FROM tb_user
        WHERE username = #{username}
        AND deleted = false
    </select>

    <!-- 根据邮箱查找用户 -->
    <select id="findByEmail" resultType="com.promanage.service.entity.User">
        SELECT * FROM tb_user
        WHERE email = #{email}
        AND deleted = false
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="existsByUsername" resultType="int">
        SELECT COUNT(1) FROM tb_user
        WHERE username = #{username}
        AND deleted = false
    </select>

    <!-- 检查邮箱是否存在 -->
    <select id="existsByEmail" resultType="int">
        SELECT COUNT(1) FROM tb_user
        WHERE email = #{email}
        AND deleted = false
    </select>

</mapper>